syntax = "proto3";

package crm;

service Login {
    rpc verify (LoginVerifyReq) returns (LoginVerifyResp);
}

message LoginVerifyReq {
   string account = 1;
   string password = 2;
}

message LoginVerifyResp {
    string id = 1;
}